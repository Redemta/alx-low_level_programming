#include "main.h"

/**
 * mul - funtion to multiply two integers
 * @a: variable to check
 * @b: another variable to check
 * Return: Always 0 (success)
 */

int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}

