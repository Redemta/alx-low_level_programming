#include "main.h"

/**
 * _abs - program to compute absolute value of integer.
 * @n: int to check.
 * Return: absolute int value.
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
